# Fictional.Measurements.Definitions

This sample package contributes fictional quantity definitions and relationships to the project
that references it. It intentionally contains no compiled quantity structs. The consuming
application's UnitsNet.Modular module chooses the definitions and generates the concrete types.

It belongs to the [shared units library sample](../README.md), which demonstrates both direct
project consumption and local NuGet package consumption.
