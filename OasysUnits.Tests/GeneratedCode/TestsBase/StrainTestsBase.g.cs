//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by \generate-code.bat.
//
//     Changes to this file will be lost when the code is regenerated.
//     The build server regenerates the code before each build and a pre-build
//     step will regenerate the code on each local build.
//
//     See https://github.com/angularsen/UnitsNet/wiki/Adding-a-New-Unit for how to add or edit units.
//
//     Add CustomCode\Quantities\MyQuantity.extra.cs files to add code to generated quantities.
//     Add UnitDefinitions\MyQuantity.json and run generate-code.bat to generate new units or quantities.
//
// </auto-generated>
//------------------------------------------------------------------------------

// Licensed under MIT No Attribution, see LICENSE file at the root.
// Copyright 2013 Andreas Gullberg Larsen (andreas.larsen84@gmail.com). Maintained at https://github.com/angularsen/UnitsNet.

using System;
using System.Collections.Generic;
using System.Globalization;
using System.Linq;
using System.Threading;
using OasysUnits.Tests.TestsBase;
using OasysUnits.Units;
using Xunit;

// Disable build warning CS1718: Comparison made to same variable; did you mean to compare something else?
#pragma warning disable 1718

// ReSharper disable once CheckNamespace
namespace OasysUnits.Tests
{
    /// <summary>
    /// Test of Strain.
    /// </summary>
// ReSharper disable once PartialTypeWithSinglePart
    public abstract partial class StrainTestsBase : QuantityTestsBase
    {
        protected abstract double MicroStrainInOneRatio { get; }
        protected abstract double MilliStrainInOneRatio { get; }
        protected abstract double PercentInOneRatio { get; }
        protected abstract double RatioInOneRatio { get; }

// ReSharper disable VirtualMemberNeverOverriden.Global
        protected virtual double MicroStrainTolerance { get { return 1e-5; } }
        protected virtual double MilliStrainTolerance { get { return 1e-5; } }
        protected virtual double PercentTolerance { get { return 1e-5; } }
        protected virtual double RatioTolerance { get { return 1e-5; } }
// ReSharper restore VirtualMemberNeverOverriden.Global

        protected (double UnitsInBaseUnit, double Tolerence) GetConversionFactor(StrainUnit unit)
        {
            return unit switch
            {
                StrainUnit.MicroStrain => (MicroStrainInOneRatio, MicroStrainTolerance),
                StrainUnit.MilliStrain => (MilliStrainInOneRatio, MilliStrainTolerance),
                StrainUnit.Percent => (PercentInOneRatio, PercentTolerance),
                StrainUnit.Ratio => (RatioInOneRatio, RatioTolerance),
                _ => throw new NotSupportedException()
            };
        }

        public static IEnumerable<object[]> UnitTypes = new List<object[]>
        {
            new object[] { StrainUnit.MicroStrain },
            new object[] { StrainUnit.MilliStrain },
            new object[] { StrainUnit.Percent },
            new object[] { StrainUnit.Ratio },
        };

        [Fact]
        public void DefaultCtor_ReturnsQuantityWithZeroValueAndBaseUnit()
        {
            var quantity = new Strain();
            Assert.Equal(0, quantity.Value);
            Assert.Equal(StrainUnit.Ratio, quantity.Unit);
        }

        [Fact]
        public void Ctor_WithInfinityValue_ThrowsArgumentException()
        {
            Assert.Throws<ArgumentException>(() => new Strain(double.PositiveInfinity, StrainUnit.Ratio));
            Assert.Throws<ArgumentException>(() => new Strain(double.NegativeInfinity, StrainUnit.Ratio));
        }

        [Fact]
        public void Ctor_WithNaNValue_ThrowsArgumentException()
        {
            Assert.Throws<ArgumentException>(() => new Strain(double.NaN, StrainUnit.Ratio));
        }

        [Fact]
        public void Ctor_NullAsUnitSystem_ThrowsArgumentNullException()
        {
            Assert.Throws<ArgumentNullException>(() => new Strain(value: 1, unitSystem: null));
        }

        [Fact]
        public void Ctor_SIUnitSystem_ThrowsArgumentExceptionIfNotSupported()
        {
            Func<object> TestCode = () => new Strain(value: 1, unitSystem: UnitSystem.SI);
            if (SupportsSIUnitSystem)
            {
                var quantity = (Strain) TestCode();
                Assert.Equal(1, quantity.Value);
            }
            else
            {
                Assert.Throws<ArgumentException>(TestCode);
            }
        }

        [Fact]
        public void Strain_QuantityInfo_ReturnsQuantityInfoDescribingQuantity()
        {
            var quantity = new Strain(1, StrainUnit.Ratio);

            QuantityInfo<StrainUnit> quantityInfo = quantity.QuantityInfo;

            Assert.Equal(Strain.Zero, quantityInfo.Zero);
            Assert.Equal("Strain", quantityInfo.Name);

            var units = EnumUtils.GetEnumValues<StrainUnit>().OrderBy(x => x.ToString()).ToArray();
            var unitNames = units.Select(x => x.ToString());
        }

        [Fact]
        public void RatioToStrainUnits()
        {
            Strain ratio = Strain.FromRatio(1);
            AssertEx.EqualTolerance(MicroStrainInOneRatio, ratio.MicroStrain, MicroStrainTolerance);
            AssertEx.EqualTolerance(MilliStrainInOneRatio, ratio.MilliStrain, MilliStrainTolerance);
            AssertEx.EqualTolerance(PercentInOneRatio, ratio.Percent, PercentTolerance);
            AssertEx.EqualTolerance(RatioInOneRatio, ratio.Ratio, RatioTolerance);
        }

        [Fact]
        public void From_ValueAndUnit_ReturnsQuantityWithSameValueAndUnit()
        {
            var quantity00 = Strain.From(1, StrainUnit.MicroStrain);
            AssertEx.EqualTolerance(1, quantity00.MicroStrain, MicroStrainTolerance);
            Assert.Equal(StrainUnit.MicroStrain, quantity00.Unit);

            var quantity01 = Strain.From(1, StrainUnit.MilliStrain);
            AssertEx.EqualTolerance(1, quantity01.MilliStrain, MilliStrainTolerance);
            Assert.Equal(StrainUnit.MilliStrain, quantity01.Unit);

            var quantity02 = Strain.From(1, StrainUnit.Percent);
            AssertEx.EqualTolerance(1, quantity02.Percent, PercentTolerance);
            Assert.Equal(StrainUnit.Percent, quantity02.Unit);

            var quantity03 = Strain.From(1, StrainUnit.Ratio);
            AssertEx.EqualTolerance(1, quantity03.Ratio, RatioTolerance);
            Assert.Equal(StrainUnit.Ratio, quantity03.Unit);

        }

        [Fact]
        public void FromRatio_WithInfinityValue_ThrowsArgumentException()
        {
            Assert.Throws<ArgumentException>(() => Strain.FromRatio(double.PositiveInfinity));
            Assert.Throws<ArgumentException>(() => Strain.FromRatio(double.NegativeInfinity));
        }

        [Fact]
        public void FromRatio_WithNanValue_ThrowsArgumentException()
        {
            Assert.Throws<ArgumentException>(() => Strain.FromRatio(double.NaN));
        }

        [Fact]
        public void As()
        {
            var ratio = Strain.FromRatio(1);
            AssertEx.EqualTolerance(MicroStrainInOneRatio, ratio.As(StrainUnit.MicroStrain), MicroStrainTolerance);
            AssertEx.EqualTolerance(MilliStrainInOneRatio, ratio.As(StrainUnit.MilliStrain), MilliStrainTolerance);
            AssertEx.EqualTolerance(PercentInOneRatio, ratio.As(StrainUnit.Percent), PercentTolerance);
            AssertEx.EqualTolerance(RatioInOneRatio, ratio.As(StrainUnit.Ratio), RatioTolerance);
        }

        [Fact]
        public void As_SIUnitSystem_ThrowsArgumentExceptionIfNotSupported()
        {
            var quantity = new Strain(value: 1, unit: Strain.BaseUnit);
            Func<object> AsWithSIUnitSystem = () => quantity.As(UnitSystem.SI);

            if (SupportsSIUnitSystem)
            {
                var value = Convert.ToDouble(AsWithSIUnitSystem());
                Assert.Equal(1, value);
            }
            else
            {
                Assert.Throws<ArgumentException>(AsWithSIUnitSystem);
            }
        }

        [Fact]
        public void Parse()
        {
            try
            {
                var parsed = Strain.Parse("1 µε", CultureInfo.GetCultureInfo("en-US"));
                AssertEx.EqualTolerance(1, parsed.MicroStrain, MicroStrainTolerance);
                Assert.Equal(StrainUnit.MicroStrain, parsed.Unit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

            try
            {
                var parsed = Strain.Parse("1 ‰", CultureInfo.GetCultureInfo("en-US"));
                AssertEx.EqualTolerance(1, parsed.MilliStrain, MilliStrainTolerance);
                Assert.Equal(StrainUnit.MilliStrain, parsed.Unit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

            try
            {
                var parsed = Strain.Parse("1 %", CultureInfo.GetCultureInfo("en-US"));
                AssertEx.EqualTolerance(1, parsed.Percent, PercentTolerance);
                Assert.Equal(StrainUnit.Percent, parsed.Unit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

            try
            {
                var parsed = Strain.Parse("1 ε", CultureInfo.GetCultureInfo("en-US"));
                AssertEx.EqualTolerance(1, parsed.Ratio, RatioTolerance);
                Assert.Equal(StrainUnit.Ratio, parsed.Unit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

        }

        [Fact]
        public void TryParse()
        {
            {
                Assert.True(Strain.TryParse("1 µε", CultureInfo.GetCultureInfo("en-US"), out var parsed));
                AssertEx.EqualTolerance(1, parsed.MicroStrain, MicroStrainTolerance);
                Assert.Equal(StrainUnit.MicroStrain, parsed.Unit);
            }

            {
                Assert.True(Strain.TryParse("1 ‰", CultureInfo.GetCultureInfo("en-US"), out var parsed));
                AssertEx.EqualTolerance(1, parsed.MilliStrain, MilliStrainTolerance);
                Assert.Equal(StrainUnit.MilliStrain, parsed.Unit);
            }

            {
                Assert.True(Strain.TryParse("1 %", CultureInfo.GetCultureInfo("en-US"), out var parsed));
                AssertEx.EqualTolerance(1, parsed.Percent, PercentTolerance);
                Assert.Equal(StrainUnit.Percent, parsed.Unit);
            }

            {
                Assert.True(Strain.TryParse("1 ε", CultureInfo.GetCultureInfo("en-US"), out var parsed));
                AssertEx.EqualTolerance(1, parsed.Ratio, RatioTolerance);
                Assert.Equal(StrainUnit.Ratio, parsed.Unit);
            }

        }

        [Fact]
        public void ParseUnit()
        {
            try
            {
                var parsedUnit = Strain.ParseUnit("µε", CultureInfo.GetCultureInfo("en-US"));
                Assert.Equal(StrainUnit.MicroStrain, parsedUnit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

            try
            {
                var parsedUnit = Strain.ParseUnit("‰", CultureInfo.GetCultureInfo("en-US"));
                Assert.Equal(StrainUnit.MilliStrain, parsedUnit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

            try
            {
                var parsedUnit = Strain.ParseUnit("%", CultureInfo.GetCultureInfo("en-US"));
                Assert.Equal(StrainUnit.Percent, parsedUnit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

            try
            {
                var parsedUnit = Strain.ParseUnit("ε", CultureInfo.GetCultureInfo("en-US"));
                Assert.Equal(StrainUnit.Ratio, parsedUnit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

        }

        [Fact]
        public void TryParseUnit()
        {
            {
                Assert.True(Strain.TryParseUnit("µε", CultureInfo.GetCultureInfo("en-US"), out var parsedUnit));
                Assert.Equal(StrainUnit.MicroStrain, parsedUnit);
            }

            {
                Assert.True(Strain.TryParseUnit("‰", CultureInfo.GetCultureInfo("en-US"), out var parsedUnit));
                Assert.Equal(StrainUnit.MilliStrain, parsedUnit);
            }

            {
                Assert.True(Strain.TryParseUnit("%", CultureInfo.GetCultureInfo("en-US"), out var parsedUnit));
                Assert.Equal(StrainUnit.Percent, parsedUnit);
            }

            {
                Assert.True(Strain.TryParseUnit("ε", CultureInfo.GetCultureInfo("en-US"), out var parsedUnit));
                Assert.Equal(StrainUnit.Ratio, parsedUnit);
            }

        }

        [Theory]
        [MemberData(nameof(UnitTypes))]
        public void ToUnit(StrainUnit unit)
        {
            var inBaseUnits = Strain.From(1.0, Strain.BaseUnit);
            var converted = inBaseUnits.ToUnit(unit);

            var conversionFactor = GetConversionFactor(unit);
            AssertEx.EqualTolerance(conversionFactor.UnitsInBaseUnit, converted.Value, conversionFactor.Tolerence);
            Assert.Equal(unit, converted.Unit);
        }

        [Theory]
        [MemberData(nameof(UnitTypes))]
        public void ToUnit_WithSameUnits_AreEqual(StrainUnit unit)
        {
            var quantity = Strain.From(3.0, unit);
            var toUnitWithSameUnit = quantity.ToUnit(unit);
            Assert.Equal(quantity, toUnitWithSameUnit);
        }

        [Theory]
        [MemberData(nameof(UnitTypes))]
        public void ToUnit_FromNonBaseUnit_ReturnsQuantityWithGivenUnit(StrainUnit unit)
        {
            // See if there is a unit available that is not the base unit, fallback to base unit if it has only a single unit.
            var fromUnit = Strain.Units.First(u => u != Strain.BaseUnit);

            var quantity = Strain.From(3.0, fromUnit);
            var converted = quantity.ToUnit(unit);
            Assert.Equal(converted.Unit, unit);
        }

        [Theory]
        [MemberData(nameof(UnitTypes))]
        public virtual void ToUnit_FromDefaultQuantity_ReturnsQuantityWithGivenUnit(StrainUnit unit)
        {
            var quantity = default(Strain);
            var converted = quantity.ToUnit(unit);
            Assert.Equal(converted.Unit, unit);
        }

        [Fact]
        public void ConversionRoundTrip()
        {
            Strain ratio = Strain.FromRatio(1);
            AssertEx.EqualTolerance(1, Strain.FromMicroStrain(ratio.MicroStrain).Ratio, MicroStrainTolerance);
            AssertEx.EqualTolerance(1, Strain.FromMilliStrain(ratio.MilliStrain).Ratio, MilliStrainTolerance);
            AssertEx.EqualTolerance(1, Strain.FromPercent(ratio.Percent).Ratio, PercentTolerance);
            AssertEx.EqualTolerance(1, Strain.FromRatio(ratio.Ratio).Ratio, RatioTolerance);
        }

        [Fact]
        public void ArithmeticOperators()
        {
            Strain v = Strain.FromRatio(1);
            AssertEx.EqualTolerance(-1, -v.Ratio, RatioTolerance);
            AssertEx.EqualTolerance(2, (Strain.FromRatio(3)-v).Ratio, RatioTolerance);
            AssertEx.EqualTolerance(2, (v + v).Ratio, RatioTolerance);
            AssertEx.EqualTolerance(10, (v*10).Ratio, RatioTolerance);
            AssertEx.EqualTolerance(10, (10*v).Ratio, RatioTolerance);
            AssertEx.EqualTolerance(2, (Strain.FromRatio(10)/5).Ratio, RatioTolerance);
            AssertEx.EqualTolerance(2, Strain.FromRatio(10)/Strain.FromRatio(5), RatioTolerance);
        }

        [Fact]
        public void ComparisonOperators()
        {
            Strain oneRatio = Strain.FromRatio(1);
            Strain twoRatio = Strain.FromRatio(2);

            Assert.True(oneRatio < twoRatio);
            Assert.True(oneRatio <= twoRatio);
            Assert.True(twoRatio > oneRatio);
            Assert.True(twoRatio >= oneRatio);

            Assert.False(oneRatio > twoRatio);
            Assert.False(oneRatio >= twoRatio);
            Assert.False(twoRatio < oneRatio);
            Assert.False(twoRatio <= oneRatio);
        }

        [Fact]
        public void CompareToIsImplemented()
        {
            Strain ratio = Strain.FromRatio(1);
            Assert.Equal(0, ratio.CompareTo(ratio));
            Assert.True(ratio.CompareTo(Strain.Zero) > 0);
            Assert.True(Strain.Zero.CompareTo(ratio) < 0);
        }

        [Fact]
        public void CompareToThrowsOnTypeMismatch()
        {
            Strain ratio = Strain.FromRatio(1);
            Assert.Throws<ArgumentException>(() => ratio.CompareTo(new object()));
        }

        [Fact]
        public void CompareToThrowsOnNull()
        {
            Strain ratio = Strain.FromRatio(1);
            Assert.Throws<ArgumentNullException>(() => ratio.CompareTo(null));
        }

        [Theory]
        [InlineData(1, StrainUnit.Ratio, 1, StrainUnit.Ratio, true)]  // Same value and unit.
        [InlineData(1, StrainUnit.Ratio, 2, StrainUnit.Ratio, false)] // Different value.
        [InlineData(2, StrainUnit.Ratio, 1, StrainUnit.MicroStrain, false)] // Different value and unit.
        [InlineData(1, StrainUnit.Ratio, 1, StrainUnit.MicroStrain, false)] // Different unit.
        public void Equals_ReturnsTrue_IfValueAndUnitAreEqual(double valueA, StrainUnit unitA, double valueB, StrainUnit unitB, bool expectEqual)
        {
            var a = new Strain(valueA, unitA);
            var b = new Strain(valueB, unitB);

            // Operator overloads.
            Assert.Equal(expectEqual, a == b);
            Assert.Equal(expectEqual, b == a);
            Assert.Equal(!expectEqual, a != b);
            Assert.Equal(!expectEqual, b != a);

            // IEquatable<T>
            Assert.Equal(expectEqual, a.Equals(b));
            Assert.Equal(expectEqual, b.Equals(a));

            // IEquatable
            Assert.Equal(expectEqual, a.Equals((object)b));
            Assert.Equal(expectEqual, b.Equals((object)a));
        }

        [Fact]
        public void Equals_Null_ReturnsFalse()
        {
            var a = Strain.Zero;

            Assert.False(a.Equals((object)null));

            // "The result of the expression is always 'false'..."
            #pragma warning disable CS8073
            Assert.False(a == null);
            Assert.False(null == a);
            Assert.True(a != null);
            Assert.True(null != a);
            #pragma warning restore CS8073
        }

        [Fact]
        public void Equals_RelativeTolerance_IsImplemented()
        {
            var v = Strain.FromRatio(1);
            Assert.True(v.Equals(Strain.FromRatio(1), RatioTolerance, ComparisonType.Relative));
            Assert.False(v.Equals(Strain.Zero, RatioTolerance, ComparisonType.Relative));
        }

        [Fact]
        public void Equals_NegativeRelativeTolerance_ThrowsArgumentOutOfRangeException()
        {
            var v = Strain.FromRatio(1);
            Assert.Throws<ArgumentOutOfRangeException>(() => v.Equals(Strain.FromRatio(1), -1, ComparisonType.Relative));
        }

        [Fact]
        public void EqualsReturnsFalseOnTypeMismatch()
        {
            Strain ratio = Strain.FromRatio(1);
            Assert.False(ratio.Equals(new object()));
        }

        [Fact]
        public void EqualsReturnsFalseOnNull()
        {
            Strain ratio = Strain.FromRatio(1);
            Assert.False(ratio.Equals(null));
        }

        [Fact]
        public void HasAtLeastOneAbbreviationSpecified()
        {
            var units = Enum.GetValues(typeof(StrainUnit)).Cast<StrainUnit>();
            foreach (var unit in units)
            {
                var defaultAbbreviation = UnitAbbreviationsCache.Default.GetDefaultAbbreviation(unit);
            }
        }

        [Fact]
        public void BaseDimensionsShouldNeverBeNull()
        {
            Assert.False(Strain.BaseDimensions is null);
        }

        [Fact]
        public void ToString_ReturnsValueAndUnitAbbreviationInCurrentCulture()
        {
            var prevCulture = Thread.CurrentThread.CurrentCulture;
            Thread.CurrentThread.CurrentCulture = CultureInfo.GetCultureInfo("en-US");
            try {
                Assert.Equal("1 µε", new Strain(1, StrainUnit.MicroStrain).ToString());
                Assert.Equal("1 ‰", new Strain(1, StrainUnit.MilliStrain).ToString());
                Assert.Equal("1 %", new Strain(1, StrainUnit.Percent).ToString());
                Assert.Equal("1 ε", new Strain(1, StrainUnit.Ratio).ToString());
            }
            finally
            {
                Thread.CurrentThread.CurrentCulture = prevCulture;
            }
        }

        [Fact]
        public void ToString_WithSwedishCulture_ReturnsUnitAbbreviationForEnglishCultureSinceThereAreNoMappings()
        {
            // Chose this culture, because we don't currently have any abbreviations mapped for that culture and we expect the en-US to be used as fallback.
            var swedishCulture = CultureInfo.GetCultureInfo("sv-SE");

            Assert.Equal("1 µε", new Strain(1, StrainUnit.MicroStrain).ToString(swedishCulture));
            Assert.Equal("1 ‰", new Strain(1, StrainUnit.MilliStrain).ToString(swedishCulture));
            Assert.Equal("1 %", new Strain(1, StrainUnit.Percent).ToString(swedishCulture));
            Assert.Equal("1 ε", new Strain(1, StrainUnit.Ratio).ToString(swedishCulture));
        }

        [Fact]
        public void ToString_SFormat_FormatsNumberWithGivenDigitsAfterRadixForCurrentCulture()
        {
            var oldCulture = CultureInfo.CurrentCulture;
            try
            {
                CultureInfo.CurrentCulture = CultureInfo.InvariantCulture;
                Assert.Equal("0.1 ε", new Strain(0.123456, StrainUnit.Ratio).ToString("s1"));
                Assert.Equal("0.12 ε", new Strain(0.123456, StrainUnit.Ratio).ToString("s2"));
                Assert.Equal("0.123 ε", new Strain(0.123456, StrainUnit.Ratio).ToString("s3"));
                Assert.Equal("0.1235 ε", new Strain(0.123456, StrainUnit.Ratio).ToString("s4"));
            }
            finally
            {
                CultureInfo.CurrentCulture = oldCulture;
            }
        }

        [Fact]
        public void ToString_SFormatAndCulture_FormatsNumberWithGivenDigitsAfterRadixForGivenCulture()
        {
            var culture = CultureInfo.InvariantCulture;
            Assert.Equal("0.1 ε", new Strain(0.123456, StrainUnit.Ratio).ToString("s1", culture));
            Assert.Equal("0.12 ε", new Strain(0.123456, StrainUnit.Ratio).ToString("s2", culture));
            Assert.Equal("0.123 ε", new Strain(0.123456, StrainUnit.Ratio).ToString("s3", culture));
            Assert.Equal("0.1235 ε", new Strain(0.123456, StrainUnit.Ratio).ToString("s4", culture));
        }

        [Theory]
        [InlineData(null)]
        [InlineData("en-US")]
        public void ToString_NullFormat_DefaultsToGeneralFormat(string cultureName)
        {
            var quantity = Strain.FromRatio(1.0);
            CultureInfo formatProvider = cultureName == null
                ? null
                : CultureInfo.GetCultureInfo(cultureName);

            Assert.Equal(quantity.ToString("g", formatProvider), quantity.ToString(null, formatProvider));
        }

        [Theory]
        [InlineData(null)]
        [InlineData("g")]
        public void ToString_NullProvider_EqualsCurrentCulture(string format)
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal(quantity.ToString(format, CultureInfo.CurrentCulture), quantity.ToString(format, null));
        }

        [Fact]
        public void Convert_ToBool_ThrowsInvalidCastException()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Throws<InvalidCastException>(() => Convert.ToBoolean(quantity));
        }

        [Fact]
        public void Convert_ToByte_EqualsValueAsSameType()
        {
            var quantity = Strain.FromRatio(1.0);
           Assert.Equal((byte)quantity.Value, Convert.ToByte(quantity));
        }

        [Fact]
        public void Convert_ToChar_ThrowsInvalidCastException()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Throws<InvalidCastException>(() => Convert.ToChar(quantity));
        }

        [Fact]
        public void Convert_ToDateTime_ThrowsInvalidCastException()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Throws<InvalidCastException>(() => Convert.ToDateTime(quantity));
        }

        [Fact]
        public void Convert_ToDecimal_EqualsValueAsSameType()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal((decimal)quantity.Value, Convert.ToDecimal(quantity));
        }

        [Fact]
        public void Convert_ToDouble_EqualsValueAsSameType()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal((double)quantity.Value, Convert.ToDouble(quantity));
        }

        [Fact]
        public void Convert_ToInt16_EqualsValueAsSameType()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal((short)quantity.Value, Convert.ToInt16(quantity));
        }

        [Fact]
        public void Convert_ToInt32_EqualsValueAsSameType()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal((int)quantity.Value, Convert.ToInt32(quantity));
        }

        [Fact]
        public void Convert_ToInt64_EqualsValueAsSameType()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal((long)quantity.Value, Convert.ToInt64(quantity));
        }

        [Fact]
        public void Convert_ToSByte_EqualsValueAsSameType()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal((sbyte)quantity.Value, Convert.ToSByte(quantity));
        }

        [Fact]
        public void Convert_ToSingle_EqualsValueAsSameType()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal((float)quantity.Value, Convert.ToSingle(quantity));
        }

        [Fact]
        public void Convert_ToString_EqualsToString()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal(quantity.ToString(), Convert.ToString(quantity));
        }

        [Fact]
        public void Convert_ToUInt16_EqualsValueAsSameType()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal((ushort)quantity.Value, Convert.ToUInt16(quantity));
        }

        [Fact]
        public void Convert_ToUInt32_EqualsValueAsSameType()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal((uint)quantity.Value, Convert.ToUInt32(quantity));
        }

        [Fact]
        public void Convert_ToUInt64_EqualsValueAsSameType()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal((ulong)quantity.Value, Convert.ToUInt64(quantity));
        }

        [Fact]
        public void Convert_ChangeType_SelfType_EqualsSelf()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal(quantity, Convert.ChangeType(quantity, typeof(Strain)));
        }

        [Fact]
        public void Convert_ChangeType_UnitType_EqualsUnit()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal(quantity.Unit, Convert.ChangeType(quantity, typeof(StrainUnit)));
        }

        [Fact]
        public void Convert_ChangeType_QuantityInfo_EqualsQuantityInfo()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal(Strain.Info, Convert.ChangeType(quantity, typeof(QuantityInfo)));
        }

        [Fact]
        public void Convert_ChangeType_BaseDimensions_EqualsBaseDimensions()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal(Strain.BaseDimensions, Convert.ChangeType(quantity, typeof(BaseDimensions)));
        }

        [Fact]
        public void Convert_ChangeType_InvalidType_ThrowsInvalidCastException()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Throws<InvalidCastException>(() => Convert.ChangeType(quantity, typeof(QuantityFormatter)));
        }

        [Fact]
        public void GetHashCode_Equals()
        {
            var quantity = Strain.FromRatio(1.0);
            Assert.Equal(new {Strain.Info.Name, quantity.Value, quantity.Unit}.GetHashCode(), quantity.GetHashCode());
        }

        [Theory]
        [InlineData(1.0)]
        [InlineData(-1.0)]
        public void NegationOperator_ReturnsQuantity_WithNegatedValue(double value)
        {
            var quantity = Strain.FromRatio(value);
            Assert.Equal(Strain.FromRatio(-value), -quantity);
        }
    }
}
