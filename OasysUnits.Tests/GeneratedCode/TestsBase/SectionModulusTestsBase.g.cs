//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by \generate-code.bat.
//
//     Changes to this file will be lost when the code is regenerated.
//     The build server regenerates the code before each build and a pre-build
//     step will regenerate the code on each local build.
//
//     See https://github.com/angularsen/UnitsNet/wiki/Adding-a-New-Unit for how to add or edit units.
//
//     Add CustomCode\Quantities\MyQuantity.extra.cs files to add code to generated quantities.
//     Add UnitDefinitions\MyQuantity.json and run generate-code.bat to generate new units or quantities.
//
// </auto-generated>
//------------------------------------------------------------------------------

// Licensed under MIT No Attribution, see LICENSE file at the root.
// Copyright 2013 Andreas Gullberg Larsen (andreas.larsen84@gmail.com). Maintained at https://github.com/angularsen/UnitsNet.

using System;
using System.Collections.Generic;
using System.Globalization;
using System.Linq;
using System.Threading;
using OasysUnits.Tests.TestsBase;
using OasysUnits.Units;
using Xunit;

// Disable build warning CS1718: Comparison made to same variable; did you mean to compare something else?
#pragma warning disable 1718

// ReSharper disable once CheckNamespace
namespace OasysUnits.Tests
{
    /// <summary>
    /// Test of SectionModulus.
    /// </summary>
// ReSharper disable once PartialTypeWithSinglePart
    public abstract partial class SectionModulusTestsBase : QuantityTestsBase
    {
        protected abstract double CubicCentimetersInOneCubicMeter { get; }
        protected abstract double CubicFeetInOneCubicMeter { get; }
        protected abstract double CubicInchesInOneCubicMeter { get; }
        protected abstract double CubicMetersInOneCubicMeter { get; }
        protected abstract double CubicMillimetersInOneCubicMeter { get; }

// ReSharper disable VirtualMemberNeverOverriden.Global
        protected virtual double CubicCentimetersTolerance { get { return 1e-5; } }
        protected virtual double CubicFeetTolerance { get { return 1e-5; } }
        protected virtual double CubicInchesTolerance { get { return 1e-5; } }
        protected virtual double CubicMetersTolerance { get { return 1e-5; } }
        protected virtual double CubicMillimetersTolerance { get { return 1e-5; } }
// ReSharper restore VirtualMemberNeverOverriden.Global

        protected (double UnitsInBaseUnit, double Tolerence) GetConversionFactor(SectionModulusUnit unit)
        {
            return unit switch
            {
                SectionModulusUnit.CubicCentimeter => (CubicCentimetersInOneCubicMeter, CubicCentimetersTolerance),
                SectionModulusUnit.CubicFoot => (CubicFeetInOneCubicMeter, CubicFeetTolerance),
                SectionModulusUnit.CubicInch => (CubicInchesInOneCubicMeter, CubicInchesTolerance),
                SectionModulusUnit.CubicMeter => (CubicMetersInOneCubicMeter, CubicMetersTolerance),
                SectionModulusUnit.CubicMillimeter => (CubicMillimetersInOneCubicMeter, CubicMillimetersTolerance),
                _ => throw new NotSupportedException()
            };
        }

        public static IEnumerable<object[]> UnitTypes = new List<object[]>
        {
            new object[] { SectionModulusUnit.CubicCentimeter },
            new object[] { SectionModulusUnit.CubicFoot },
            new object[] { SectionModulusUnit.CubicInch },
            new object[] { SectionModulusUnit.CubicMeter },
            new object[] { SectionModulusUnit.CubicMillimeter },
        };

        [Fact]
        public void DefaultCtor_ReturnsQuantityWithZeroValueAndBaseUnit()
        {
            var quantity = new SectionModulus();
            Assert.Equal(0, quantity.Value);
            Assert.Equal(SectionModulusUnit.CubicMeter, quantity.Unit);
        }

        [Fact]
        public void Ctor_WithInfinityValue_ThrowsArgumentException()
        {
            Assert.Throws<ArgumentException>(() => new SectionModulus(double.PositiveInfinity, SectionModulusUnit.CubicMeter));
            Assert.Throws<ArgumentException>(() => new SectionModulus(double.NegativeInfinity, SectionModulusUnit.CubicMeter));
        }

        [Fact]
        public void Ctor_WithNaNValue_ThrowsArgumentException()
        {
            Assert.Throws<ArgumentException>(() => new SectionModulus(double.NaN, SectionModulusUnit.CubicMeter));
        }

        [Fact]
        public void Ctor_NullAsUnitSystem_ThrowsArgumentNullException()
        {
            Assert.Throws<ArgumentNullException>(() => new SectionModulus(value: 1, unitSystem: null));
        }

        [Fact]
        public void Ctor_SIUnitSystem_ThrowsArgumentExceptionIfNotSupported()
        {
            Func<object> TestCode = () => new SectionModulus(value: 1, unitSystem: UnitSystem.SI);
            if (SupportsSIUnitSystem)
            {
                var quantity = (SectionModulus) TestCode();
                Assert.Equal(1, quantity.Value);
            }
            else
            {
                Assert.Throws<ArgumentException>(TestCode);
            }
        }

        [Fact]
        public void SectionModulus_QuantityInfo_ReturnsQuantityInfoDescribingQuantity()
        {
            var quantity = new SectionModulus(1, SectionModulusUnit.CubicMeter);

            QuantityInfo<SectionModulusUnit> quantityInfo = quantity.QuantityInfo;

            Assert.Equal(SectionModulus.Zero, quantityInfo.Zero);
            Assert.Equal("SectionModulus", quantityInfo.Name);

            var units = EnumUtils.GetEnumValues<SectionModulusUnit>().OrderBy(x => x.ToString()).ToArray();
            var unitNames = units.Select(x => x.ToString());
        }

        [Fact]
        public void CubicMeterToSectionModulusUnits()
        {
            SectionModulus cubicmeter = SectionModulus.FromCubicMeters(1);
            AssertEx.EqualTolerance(CubicCentimetersInOneCubicMeter, cubicmeter.CubicCentimeters, CubicCentimetersTolerance);
            AssertEx.EqualTolerance(CubicFeetInOneCubicMeter, cubicmeter.CubicFeet, CubicFeetTolerance);
            AssertEx.EqualTolerance(CubicInchesInOneCubicMeter, cubicmeter.CubicInches, CubicInchesTolerance);
            AssertEx.EqualTolerance(CubicMetersInOneCubicMeter, cubicmeter.CubicMeters, CubicMetersTolerance);
            AssertEx.EqualTolerance(CubicMillimetersInOneCubicMeter, cubicmeter.CubicMillimeters, CubicMillimetersTolerance);
        }

        [Fact]
        public void From_ValueAndUnit_ReturnsQuantityWithSameValueAndUnit()
        {
            var quantity00 = SectionModulus.From(1, SectionModulusUnit.CubicCentimeter);
            AssertEx.EqualTolerance(1, quantity00.CubicCentimeters, CubicCentimetersTolerance);
            Assert.Equal(SectionModulusUnit.CubicCentimeter, quantity00.Unit);

            var quantity01 = SectionModulus.From(1, SectionModulusUnit.CubicFoot);
            AssertEx.EqualTolerance(1, quantity01.CubicFeet, CubicFeetTolerance);
            Assert.Equal(SectionModulusUnit.CubicFoot, quantity01.Unit);

            var quantity02 = SectionModulus.From(1, SectionModulusUnit.CubicInch);
            AssertEx.EqualTolerance(1, quantity02.CubicInches, CubicInchesTolerance);
            Assert.Equal(SectionModulusUnit.CubicInch, quantity02.Unit);

            var quantity03 = SectionModulus.From(1, SectionModulusUnit.CubicMeter);
            AssertEx.EqualTolerance(1, quantity03.CubicMeters, CubicMetersTolerance);
            Assert.Equal(SectionModulusUnit.CubicMeter, quantity03.Unit);

            var quantity04 = SectionModulus.From(1, SectionModulusUnit.CubicMillimeter);
            AssertEx.EqualTolerance(1, quantity04.CubicMillimeters, CubicMillimetersTolerance);
            Assert.Equal(SectionModulusUnit.CubicMillimeter, quantity04.Unit);

        }

        [Fact]
        public void FromCubicMeters_WithInfinityValue_ThrowsArgumentException()
        {
            Assert.Throws<ArgumentException>(() => SectionModulus.FromCubicMeters(double.PositiveInfinity));
            Assert.Throws<ArgumentException>(() => SectionModulus.FromCubicMeters(double.NegativeInfinity));
        }

        [Fact]
        public void FromCubicMeters_WithNanValue_ThrowsArgumentException()
        {
            Assert.Throws<ArgumentException>(() => SectionModulus.FromCubicMeters(double.NaN));
        }

        [Fact]
        public void As()
        {
            var cubicmeter = SectionModulus.FromCubicMeters(1);
            AssertEx.EqualTolerance(CubicCentimetersInOneCubicMeter, cubicmeter.As(SectionModulusUnit.CubicCentimeter), CubicCentimetersTolerance);
            AssertEx.EqualTolerance(CubicFeetInOneCubicMeter, cubicmeter.As(SectionModulusUnit.CubicFoot), CubicFeetTolerance);
            AssertEx.EqualTolerance(CubicInchesInOneCubicMeter, cubicmeter.As(SectionModulusUnit.CubicInch), CubicInchesTolerance);
            AssertEx.EqualTolerance(CubicMetersInOneCubicMeter, cubicmeter.As(SectionModulusUnit.CubicMeter), CubicMetersTolerance);
            AssertEx.EqualTolerance(CubicMillimetersInOneCubicMeter, cubicmeter.As(SectionModulusUnit.CubicMillimeter), CubicMillimetersTolerance);
        }

        [Fact]
        public void As_SIUnitSystem_ThrowsArgumentExceptionIfNotSupported()
        {
            var quantity = new SectionModulus(value: 1, unit: SectionModulus.BaseUnit);
            Func<object> AsWithSIUnitSystem = () => quantity.As(UnitSystem.SI);

            if (SupportsSIUnitSystem)
            {
                var value = Convert.ToDouble(AsWithSIUnitSystem());
                Assert.Equal(1, value);
            }
            else
            {
                Assert.Throws<ArgumentException>(AsWithSIUnitSystem);
            }
        }

        [Fact]
        public void Parse()
        {
            try
            {
                var parsed = SectionModulus.Parse("1 cm³", CultureInfo.GetCultureInfo("en-US"));
                AssertEx.EqualTolerance(1, parsed.CubicCentimeters, CubicCentimetersTolerance);
                Assert.Equal(SectionModulusUnit.CubicCentimeter, parsed.Unit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

            try
            {
                var parsed = SectionModulus.Parse("1 ft³", CultureInfo.GetCultureInfo("en-US"));
                AssertEx.EqualTolerance(1, parsed.CubicFeet, CubicFeetTolerance);
                Assert.Equal(SectionModulusUnit.CubicFoot, parsed.Unit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

            try
            {
                var parsed = SectionModulus.Parse("1 in³", CultureInfo.GetCultureInfo("en-US"));
                AssertEx.EqualTolerance(1, parsed.CubicInches, CubicInchesTolerance);
                Assert.Equal(SectionModulusUnit.CubicInch, parsed.Unit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

            try
            {
                var parsed = SectionModulus.Parse("1 m³", CultureInfo.GetCultureInfo("en-US"));
                AssertEx.EqualTolerance(1, parsed.CubicMeters, CubicMetersTolerance);
                Assert.Equal(SectionModulusUnit.CubicMeter, parsed.Unit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

            try
            {
                var parsed = SectionModulus.Parse("1 mm³", CultureInfo.GetCultureInfo("en-US"));
                AssertEx.EqualTolerance(1, parsed.CubicMillimeters, CubicMillimetersTolerance);
                Assert.Equal(SectionModulusUnit.CubicMillimeter, parsed.Unit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

        }

        [Fact]
        public void TryParse()
        {
            {
                Assert.True(SectionModulus.TryParse("1 cm³", CultureInfo.GetCultureInfo("en-US"), out var parsed));
                AssertEx.EqualTolerance(1, parsed.CubicCentimeters, CubicCentimetersTolerance);
                Assert.Equal(SectionModulusUnit.CubicCentimeter, parsed.Unit);
            }

            {
                Assert.True(SectionModulus.TryParse("1 ft³", CultureInfo.GetCultureInfo("en-US"), out var parsed));
                AssertEx.EqualTolerance(1, parsed.CubicFeet, CubicFeetTolerance);
                Assert.Equal(SectionModulusUnit.CubicFoot, parsed.Unit);
            }

            {
                Assert.True(SectionModulus.TryParse("1 in³", CultureInfo.GetCultureInfo("en-US"), out var parsed));
                AssertEx.EqualTolerance(1, parsed.CubicInches, CubicInchesTolerance);
                Assert.Equal(SectionModulusUnit.CubicInch, parsed.Unit);
            }

            {
                Assert.True(SectionModulus.TryParse("1 m³", CultureInfo.GetCultureInfo("en-US"), out var parsed));
                AssertEx.EqualTolerance(1, parsed.CubicMeters, CubicMetersTolerance);
                Assert.Equal(SectionModulusUnit.CubicMeter, parsed.Unit);
            }

            {
                Assert.True(SectionModulus.TryParse("1 mm³", CultureInfo.GetCultureInfo("en-US"), out var parsed));
                AssertEx.EqualTolerance(1, parsed.CubicMillimeters, CubicMillimetersTolerance);
                Assert.Equal(SectionModulusUnit.CubicMillimeter, parsed.Unit);
            }

        }

        [Fact]
        public void ParseUnit()
        {
            try
            {
                var parsedUnit = SectionModulus.ParseUnit("cm³", CultureInfo.GetCultureInfo("en-US"));
                Assert.Equal(SectionModulusUnit.CubicCentimeter, parsedUnit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

            try
            {
                var parsedUnit = SectionModulus.ParseUnit("ft³", CultureInfo.GetCultureInfo("en-US"));
                Assert.Equal(SectionModulusUnit.CubicFoot, parsedUnit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

            try
            {
                var parsedUnit = SectionModulus.ParseUnit("in³", CultureInfo.GetCultureInfo("en-US"));
                Assert.Equal(SectionModulusUnit.CubicInch, parsedUnit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

            try
            {
                var parsedUnit = SectionModulus.ParseUnit("m³", CultureInfo.GetCultureInfo("en-US"));
                Assert.Equal(SectionModulusUnit.CubicMeter, parsedUnit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

            try
            {
                var parsedUnit = SectionModulus.ParseUnit("mm³", CultureInfo.GetCultureInfo("en-US"));
                Assert.Equal(SectionModulusUnit.CubicMillimeter, parsedUnit);
            } catch (AmbiguousUnitParseException) { /* Some units have the same abbreviations */ }

        }

        [Fact]
        public void TryParseUnit()
        {
            {
                Assert.True(SectionModulus.TryParseUnit("cm³", CultureInfo.GetCultureInfo("en-US"), out var parsedUnit));
                Assert.Equal(SectionModulusUnit.CubicCentimeter, parsedUnit);
            }

            {
                Assert.True(SectionModulus.TryParseUnit("ft³", CultureInfo.GetCultureInfo("en-US"), out var parsedUnit));
                Assert.Equal(SectionModulusUnit.CubicFoot, parsedUnit);
            }

            {
                Assert.True(SectionModulus.TryParseUnit("in³", CultureInfo.GetCultureInfo("en-US"), out var parsedUnit));
                Assert.Equal(SectionModulusUnit.CubicInch, parsedUnit);
            }

            {
                Assert.True(SectionModulus.TryParseUnit("m³", CultureInfo.GetCultureInfo("en-US"), out var parsedUnit));
                Assert.Equal(SectionModulusUnit.CubicMeter, parsedUnit);
            }

            {
                Assert.True(SectionModulus.TryParseUnit("mm³", CultureInfo.GetCultureInfo("en-US"), out var parsedUnit));
                Assert.Equal(SectionModulusUnit.CubicMillimeter, parsedUnit);
            }

        }

        [Theory]
        [MemberData(nameof(UnitTypes))]
        public void ToUnit(SectionModulusUnit unit)
        {
            var inBaseUnits = SectionModulus.From(1.0, SectionModulus.BaseUnit);
            var converted = inBaseUnits.ToUnit(unit);

            var conversionFactor = GetConversionFactor(unit);
            AssertEx.EqualTolerance(conversionFactor.UnitsInBaseUnit, converted.Value, conversionFactor.Tolerence);
            Assert.Equal(unit, converted.Unit);
        }

        [Theory]
        [MemberData(nameof(UnitTypes))]
        public void ToUnit_WithSameUnits_AreEqual(SectionModulusUnit unit)
        {
            var quantity = SectionModulus.From(3.0, unit);
            var toUnitWithSameUnit = quantity.ToUnit(unit);
            Assert.Equal(quantity, toUnitWithSameUnit);
        }

        [Theory]
        [MemberData(nameof(UnitTypes))]
        public void ToUnit_FromNonBaseUnit_ReturnsQuantityWithGivenUnit(SectionModulusUnit unit)
        {
            // See if there is a unit available that is not the base unit, fallback to base unit if it has only a single unit.
            var fromUnit = SectionModulus.Units.First(u => u != SectionModulus.BaseUnit);

            var quantity = SectionModulus.From(3.0, fromUnit);
            var converted = quantity.ToUnit(unit);
            Assert.Equal(converted.Unit, unit);
        }

        [Theory]
        [MemberData(nameof(UnitTypes))]
        public virtual void ToUnit_FromDefaultQuantity_ReturnsQuantityWithGivenUnit(SectionModulusUnit unit)
        {
            var quantity = default(SectionModulus);
            var converted = quantity.ToUnit(unit);
            Assert.Equal(converted.Unit, unit);
        }

        [Fact]
        public void ConversionRoundTrip()
        {
            SectionModulus cubicmeter = SectionModulus.FromCubicMeters(1);
            AssertEx.EqualTolerance(1, SectionModulus.FromCubicCentimeters(cubicmeter.CubicCentimeters).CubicMeters, CubicCentimetersTolerance);
            AssertEx.EqualTolerance(1, SectionModulus.FromCubicFeet(cubicmeter.CubicFeet).CubicMeters, CubicFeetTolerance);
            AssertEx.EqualTolerance(1, SectionModulus.FromCubicInches(cubicmeter.CubicInches).CubicMeters, CubicInchesTolerance);
            AssertEx.EqualTolerance(1, SectionModulus.FromCubicMeters(cubicmeter.CubicMeters).CubicMeters, CubicMetersTolerance);
            AssertEx.EqualTolerance(1, SectionModulus.FromCubicMillimeters(cubicmeter.CubicMillimeters).CubicMeters, CubicMillimetersTolerance);
        }

        [Fact]
        public void ArithmeticOperators()
        {
            SectionModulus v = SectionModulus.FromCubicMeters(1);
            AssertEx.EqualTolerance(-1, -v.CubicMeters, CubicMetersTolerance);
            AssertEx.EqualTolerance(2, (SectionModulus.FromCubicMeters(3)-v).CubicMeters, CubicMetersTolerance);
            AssertEx.EqualTolerance(2, (v + v).CubicMeters, CubicMetersTolerance);
            AssertEx.EqualTolerance(10, (v*10).CubicMeters, CubicMetersTolerance);
            AssertEx.EqualTolerance(10, (10*v).CubicMeters, CubicMetersTolerance);
            AssertEx.EqualTolerance(2, (SectionModulus.FromCubicMeters(10)/5).CubicMeters, CubicMetersTolerance);
            AssertEx.EqualTolerance(2, SectionModulus.FromCubicMeters(10)/SectionModulus.FromCubicMeters(5), CubicMetersTolerance);
        }

        [Fact]
        public void ComparisonOperators()
        {
            SectionModulus oneCubicMeter = SectionModulus.FromCubicMeters(1);
            SectionModulus twoCubicMeters = SectionModulus.FromCubicMeters(2);

            Assert.True(oneCubicMeter < twoCubicMeters);
            Assert.True(oneCubicMeter <= twoCubicMeters);
            Assert.True(twoCubicMeters > oneCubicMeter);
            Assert.True(twoCubicMeters >= oneCubicMeter);

            Assert.False(oneCubicMeter > twoCubicMeters);
            Assert.False(oneCubicMeter >= twoCubicMeters);
            Assert.False(twoCubicMeters < oneCubicMeter);
            Assert.False(twoCubicMeters <= oneCubicMeter);
        }

        [Fact]
        public void CompareToIsImplemented()
        {
            SectionModulus cubicmeter = SectionModulus.FromCubicMeters(1);
            Assert.Equal(0, cubicmeter.CompareTo(cubicmeter));
            Assert.True(cubicmeter.CompareTo(SectionModulus.Zero) > 0);
            Assert.True(SectionModulus.Zero.CompareTo(cubicmeter) < 0);
        }

        [Fact]
        public void CompareToThrowsOnTypeMismatch()
        {
            SectionModulus cubicmeter = SectionModulus.FromCubicMeters(1);
            Assert.Throws<ArgumentException>(() => cubicmeter.CompareTo(new object()));
        }

        [Fact]
        public void CompareToThrowsOnNull()
        {
            SectionModulus cubicmeter = SectionModulus.FromCubicMeters(1);
            Assert.Throws<ArgumentNullException>(() => cubicmeter.CompareTo(null));
        }

        [Theory]
        [InlineData(1, SectionModulusUnit.CubicMeter, 1, SectionModulusUnit.CubicMeter, true)]  // Same value and unit.
        [InlineData(1, SectionModulusUnit.CubicMeter, 2, SectionModulusUnit.CubicMeter, false)] // Different value.
        [InlineData(2, SectionModulusUnit.CubicMeter, 1, SectionModulusUnit.CubicCentimeter, false)] // Different value and unit.
        [InlineData(1, SectionModulusUnit.CubicMeter, 1, SectionModulusUnit.CubicCentimeter, false)] // Different unit.
        public void Equals_ReturnsTrue_IfValueAndUnitAreEqual(double valueA, SectionModulusUnit unitA, double valueB, SectionModulusUnit unitB, bool expectEqual)
        {
            var a = new SectionModulus(valueA, unitA);
            var b = new SectionModulus(valueB, unitB);

            // Operator overloads.
            Assert.Equal(expectEqual, a == b);
            Assert.Equal(expectEqual, b == a);
            Assert.Equal(!expectEqual, a != b);
            Assert.Equal(!expectEqual, b != a);

            // IEquatable<T>
            Assert.Equal(expectEqual, a.Equals(b));
            Assert.Equal(expectEqual, b.Equals(a));

            // IEquatable
            Assert.Equal(expectEqual, a.Equals((object)b));
            Assert.Equal(expectEqual, b.Equals((object)a));
        }

        [Fact]
        public void Equals_Null_ReturnsFalse()
        {
            var a = SectionModulus.Zero;

            Assert.False(a.Equals((object)null));

            // "The result of the expression is always 'false'..."
            #pragma warning disable CS8073
            Assert.False(a == null);
            Assert.False(null == a);
            Assert.True(a != null);
            Assert.True(null != a);
            #pragma warning restore CS8073
        }

        [Fact]
        public void Equals_RelativeTolerance_IsImplemented()
        {
            var v = SectionModulus.FromCubicMeters(1);
            Assert.True(v.Equals(SectionModulus.FromCubicMeters(1), CubicMetersTolerance, ComparisonType.Relative));
            Assert.False(v.Equals(SectionModulus.Zero, CubicMetersTolerance, ComparisonType.Relative));
        }

        [Fact]
        public void Equals_NegativeRelativeTolerance_ThrowsArgumentOutOfRangeException()
        {
            var v = SectionModulus.FromCubicMeters(1);
            Assert.Throws<ArgumentOutOfRangeException>(() => v.Equals(SectionModulus.FromCubicMeters(1), -1, ComparisonType.Relative));
        }

        [Fact]
        public void EqualsReturnsFalseOnTypeMismatch()
        {
            SectionModulus cubicmeter = SectionModulus.FromCubicMeters(1);
            Assert.False(cubicmeter.Equals(new object()));
        }

        [Fact]
        public void EqualsReturnsFalseOnNull()
        {
            SectionModulus cubicmeter = SectionModulus.FromCubicMeters(1);
            Assert.False(cubicmeter.Equals(null));
        }

        [Fact]
        public void HasAtLeastOneAbbreviationSpecified()
        {
            var units = Enum.GetValues(typeof(SectionModulusUnit)).Cast<SectionModulusUnit>();
            foreach (var unit in units)
            {
                var defaultAbbreviation = UnitAbbreviationsCache.Default.GetDefaultAbbreviation(unit);
            }
        }

        [Fact]
        public void BaseDimensionsShouldNeverBeNull()
        {
            Assert.False(SectionModulus.BaseDimensions is null);
        }

        [Fact]
        public void ToString_ReturnsValueAndUnitAbbreviationInCurrentCulture()
        {
            var prevCulture = Thread.CurrentThread.CurrentCulture;
            Thread.CurrentThread.CurrentCulture = CultureInfo.GetCultureInfo("en-US");
            try {
                Assert.Equal("1 cm³", new SectionModulus(1, SectionModulusUnit.CubicCentimeter).ToString());
                Assert.Equal("1 ft³", new SectionModulus(1, SectionModulusUnit.CubicFoot).ToString());
                Assert.Equal("1 in³", new SectionModulus(1, SectionModulusUnit.CubicInch).ToString());
                Assert.Equal("1 m³", new SectionModulus(1, SectionModulusUnit.CubicMeter).ToString());
                Assert.Equal("1 mm³", new SectionModulus(1, SectionModulusUnit.CubicMillimeter).ToString());
            }
            finally
            {
                Thread.CurrentThread.CurrentCulture = prevCulture;
            }
        }

        [Fact]
        public void ToString_WithSwedishCulture_ReturnsUnitAbbreviationForEnglishCultureSinceThereAreNoMappings()
        {
            // Chose this culture, because we don't currently have any abbreviations mapped for that culture and we expect the en-US to be used as fallback.
            var swedishCulture = CultureInfo.GetCultureInfo("sv-SE");

            Assert.Equal("1 cm³", new SectionModulus(1, SectionModulusUnit.CubicCentimeter).ToString(swedishCulture));
            Assert.Equal("1 ft³", new SectionModulus(1, SectionModulusUnit.CubicFoot).ToString(swedishCulture));
            Assert.Equal("1 in³", new SectionModulus(1, SectionModulusUnit.CubicInch).ToString(swedishCulture));
            Assert.Equal("1 m³", new SectionModulus(1, SectionModulusUnit.CubicMeter).ToString(swedishCulture));
            Assert.Equal("1 mm³", new SectionModulus(1, SectionModulusUnit.CubicMillimeter).ToString(swedishCulture));
        }

        [Fact]
        public void ToString_SFormat_FormatsNumberWithGivenDigitsAfterRadixForCurrentCulture()
        {
            var oldCulture = CultureInfo.CurrentCulture;
            try
            {
                CultureInfo.CurrentCulture = CultureInfo.InvariantCulture;
                Assert.Equal("0.1 m³", new SectionModulus(0.123456, SectionModulusUnit.CubicMeter).ToString("s1"));
                Assert.Equal("0.12 m³", new SectionModulus(0.123456, SectionModulusUnit.CubicMeter).ToString("s2"));
                Assert.Equal("0.123 m³", new SectionModulus(0.123456, SectionModulusUnit.CubicMeter).ToString("s3"));
                Assert.Equal("0.1235 m³", new SectionModulus(0.123456, SectionModulusUnit.CubicMeter).ToString("s4"));
            }
            finally
            {
                CultureInfo.CurrentCulture = oldCulture;
            }
        }

        [Fact]
        public void ToString_SFormatAndCulture_FormatsNumberWithGivenDigitsAfterRadixForGivenCulture()
        {
            var culture = CultureInfo.InvariantCulture;
            Assert.Equal("0.1 m³", new SectionModulus(0.123456, SectionModulusUnit.CubicMeter).ToString("s1", culture));
            Assert.Equal("0.12 m³", new SectionModulus(0.123456, SectionModulusUnit.CubicMeter).ToString("s2", culture));
            Assert.Equal("0.123 m³", new SectionModulus(0.123456, SectionModulusUnit.CubicMeter).ToString("s3", culture));
            Assert.Equal("0.1235 m³", new SectionModulus(0.123456, SectionModulusUnit.CubicMeter).ToString("s4", culture));
        }

        [Theory]
        [InlineData(null)]
        [InlineData("en-US")]
        public void ToString_NullFormat_DefaultsToGeneralFormat(string cultureName)
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            CultureInfo formatProvider = cultureName == null
                ? null
                : CultureInfo.GetCultureInfo(cultureName);

            Assert.Equal(quantity.ToString("g", formatProvider), quantity.ToString(null, formatProvider));
        }

        [Theory]
        [InlineData(null)]
        [InlineData("g")]
        public void ToString_NullProvider_EqualsCurrentCulture(string format)
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal(quantity.ToString(format, CultureInfo.CurrentCulture), quantity.ToString(format, null));
        }

        [Fact]
        public void Convert_ToBool_ThrowsInvalidCastException()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Throws<InvalidCastException>(() => Convert.ToBoolean(quantity));
        }

        [Fact]
        public void Convert_ToByte_EqualsValueAsSameType()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
           Assert.Equal((byte)quantity.Value, Convert.ToByte(quantity));
        }

        [Fact]
        public void Convert_ToChar_ThrowsInvalidCastException()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Throws<InvalidCastException>(() => Convert.ToChar(quantity));
        }

        [Fact]
        public void Convert_ToDateTime_ThrowsInvalidCastException()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Throws<InvalidCastException>(() => Convert.ToDateTime(quantity));
        }

        [Fact]
        public void Convert_ToDecimal_EqualsValueAsSameType()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal((decimal)quantity.Value, Convert.ToDecimal(quantity));
        }

        [Fact]
        public void Convert_ToDouble_EqualsValueAsSameType()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal((double)quantity.Value, Convert.ToDouble(quantity));
        }

        [Fact]
        public void Convert_ToInt16_EqualsValueAsSameType()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal((short)quantity.Value, Convert.ToInt16(quantity));
        }

        [Fact]
        public void Convert_ToInt32_EqualsValueAsSameType()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal((int)quantity.Value, Convert.ToInt32(quantity));
        }

        [Fact]
        public void Convert_ToInt64_EqualsValueAsSameType()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal((long)quantity.Value, Convert.ToInt64(quantity));
        }

        [Fact]
        public void Convert_ToSByte_EqualsValueAsSameType()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal((sbyte)quantity.Value, Convert.ToSByte(quantity));
        }

        [Fact]
        public void Convert_ToSingle_EqualsValueAsSameType()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal((float)quantity.Value, Convert.ToSingle(quantity));
        }

        [Fact]
        public void Convert_ToString_EqualsToString()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal(quantity.ToString(), Convert.ToString(quantity));
        }

        [Fact]
        public void Convert_ToUInt16_EqualsValueAsSameType()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal((ushort)quantity.Value, Convert.ToUInt16(quantity));
        }

        [Fact]
        public void Convert_ToUInt32_EqualsValueAsSameType()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal((uint)quantity.Value, Convert.ToUInt32(quantity));
        }

        [Fact]
        public void Convert_ToUInt64_EqualsValueAsSameType()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal((ulong)quantity.Value, Convert.ToUInt64(quantity));
        }

        [Fact]
        public void Convert_ChangeType_SelfType_EqualsSelf()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal(quantity, Convert.ChangeType(quantity, typeof(SectionModulus)));
        }

        [Fact]
        public void Convert_ChangeType_UnitType_EqualsUnit()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal(quantity.Unit, Convert.ChangeType(quantity, typeof(SectionModulusUnit)));
        }

        [Fact]
        public void Convert_ChangeType_QuantityInfo_EqualsQuantityInfo()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal(SectionModulus.Info, Convert.ChangeType(quantity, typeof(QuantityInfo)));
        }

        [Fact]
        public void Convert_ChangeType_BaseDimensions_EqualsBaseDimensions()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal(SectionModulus.BaseDimensions, Convert.ChangeType(quantity, typeof(BaseDimensions)));
        }

        [Fact]
        public void Convert_ChangeType_InvalidType_ThrowsInvalidCastException()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Throws<InvalidCastException>(() => Convert.ChangeType(quantity, typeof(QuantityFormatter)));
        }

        [Fact]
        public void GetHashCode_Equals()
        {
            var quantity = SectionModulus.FromCubicMeters(1.0);
            Assert.Equal(new {SectionModulus.Info.Name, quantity.Value, quantity.Unit}.GetHashCode(), quantity.GetHashCode());
        }

        [Theory]
        [InlineData(1.0)]
        [InlineData(-1.0)]
        public void NegationOperator_ReturnsQuantity_WithNegatedValue(double value)
        {
            var quantity = SectionModulus.FromCubicMeters(value);
            Assert.Equal(SectionModulus.FromCubicMeters(-value), -quantity);
        }
    }
}
