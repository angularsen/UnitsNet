# Fictional.Measurements.Definitions

This sample package contributes public quantity specs, fictional JSON definitions, and
relationships to the project that references it. It intentionally contains no compiled quantity
structs. The consuming application's UnitsNet.Modular module chooses the specs and generates the
concrete types.
